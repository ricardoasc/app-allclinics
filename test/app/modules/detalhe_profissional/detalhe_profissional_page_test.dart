import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:allclinics/app/modules/detalhe_profissional/detalhe_profissional_page.dart';

main() {
  testWidgets('DetalheProfissionalPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(DetalheProfissionalPage(title: 'DetalheProfissional')));
    //  final titleFinder = find.text('DetalheProfissional');
    //  expect(titleFinder, findsOneWidget);
  });
}
